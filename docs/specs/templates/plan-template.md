# Plan — <NOME DA PÁGINA/FLUXO>

> Ref. spec: `./spec.md`

## 1. Visão técnica
Como a página/fluxo é implementada em alto nível.

## 2. Rotas e handlers
| Rota | Método | Handler (`app.py`) | Entrada | Saída |
|------|--------|--------------------|---------|-------|

## 3. Módulos do pipeline envolvidos
Lista de `pipeline/*.py`, `tools/*.py`, `crew.py` e funções chamadas.

## 4. Contratos de dados
Formato do `pipeline_data` / JSON persistido / payload de resposta.

## 5. Fluxo de execução
Passo a passo (detecção → crew → consolidação → persistência → render).

## 6. Decisões de design
Escolhas relevantes e alternativas descartadas.

## 7. Riscos e mitigações
- Risco → mitigação.

## 8. Aderência à constituição
Como o plano respeita os artigos aplicáveis.
